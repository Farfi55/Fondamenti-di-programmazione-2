#include "Grafo.h"

int main(int argc, char const* argv[])
{
    Grafo g(10);
    // ....
    g(i, j, true); // inserisce l'arco
    g(i, j, false); // toglie l'arco

    f(g(i, j)); ....
        return 0;
}
